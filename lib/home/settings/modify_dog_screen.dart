import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ModifyDogScreen extends StatelessWidget {
  const ModifyDogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [
          // Spacer(),
          // TextInput(textEditingController: null,),
          // Spacer(),
          // TextInput(textEditingController: textEditingController),
          // Spacer(),
          // Button(onPressed: onPressed, text: text),
          // Spacer(),
        ],
      ),
    );
  }
}
